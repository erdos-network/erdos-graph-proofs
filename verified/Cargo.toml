[package]
name = "erdos-proofs"
version = "0.1.0"
edition = "2021"

[dependencies]
# Reference the erdos-graph submodule
erdos-graph = { path = "../erdos-graph" }

# Verus standard library (includes builtin, builtin_macros, etc.)
vstd = { path = "../tools/verus-bin/vstd" }
